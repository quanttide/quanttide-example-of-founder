# AGENTS.md

## 实验室定位

量潮创始人实验室 — 将方法论文档翻译为可执行程序的实验场。

## 记忆分层原则

- 规格归 `docs/`：方法论、流程、规则。方法论的沉淀物是规格与数据，不是代码
- 数据归 `data/`：案例、语料、工作素材。只放程序输入，不混入方法论述
- 代码是验证手段：按需快速搭建，验证结束或失效后直接删除，不追求长期维护

## 代码快更迭原则

- 实验室代码应当很快更迭：代码只服务于当前一轮验证，生命周期以实验为单位，不以版本为单位
- 有效的产出从代码中沉淀回 `docs/`（规则、流程）与 `data/`（样本、语料），沉淀完成即可废弃代码
- 不为代码做前瞻性架构、不积累兼容负担；重建一个工具的成本低于维护一个过时工具

## Agent 工作原则

- `docs/` 中每篇是可执行的规则：新实验可直接在对话或临时代码中套用，验证后再评估是否需要沉淀回本篇
- `data/` 内容演进方向是结构化，新增文件时优先考虑机器可读格式或明确的结构约定
- 若实现功能，以对应 `docs/` 篇目为验收标准，不跳步、不臆造规格；验证后按快更迭原则处置
- 实验操作记录到实验日志
- 保持文档与实际进展一致

## 临时文件规矩

- 实验产生的临时文件（/tmp 下的运行产物：引擎输出、评估结果、中间素材）**必须整理进 `data/` 对应主题目录后提交**——数据要可跟踪，不留散落在仓库外的孤儿文件
- 命名：`data/{主题}/{日期}-{描述}.md`（如 `data/creation-talk/2026-09-05-涌现而非设计.md`）
- 提交前检查：本次实验动了 /tmp 的，先归档再提交，仓库外不留副本
