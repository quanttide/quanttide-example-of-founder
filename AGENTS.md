# AGENTS.md

## 实验室定位

量潮创始人实验室 — 将方法论文档翻译为可执行程序的实验场。

## 记忆分层原则

- 规格归 `docs/`：方法论、流程、规则。修改前先确认是否影响已有实现
- 数据归 `data/`：案例、语料、工作素材。只放程序输入，不混入方法论述
- 实现归 `src/`：代码以 `docs/` 为规格，以 `data/` 为输入

## Agent 工作原则

- `docs/` 中每篇视为待实现规格：新增或修改时，同步评估是否需要更新代码
- `data/` 内容演进方向是结构化，新增文件时优先考虑机器可读格式或明确的结构约定
- 实现功能以对应 `docs/` 篇目为验收标准，不跳步、不臆造规格
- 实验操作记录到实验日志
- 保持文档与实际进展一致
